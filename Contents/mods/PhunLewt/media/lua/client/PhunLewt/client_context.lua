if isServer() then
    return
end
local Core = PhunLewt
local mainName = "PhunLewt"
function Core:showContext(playerIndex, context, worldobjects)

    -- if isAdmin() or isDebugEnabled() then
    --     local player = getSpecificPlayer(playerIndex) or getPlayer()
    --     local option = context:addOptionOnTop(Core.name, context, function()
    --         Core.showLoadingModal()
    --         sendClientCommand(Core.name, Core.commands.requestNames, {})
    --     end, playerIndex)
    -- end

end
